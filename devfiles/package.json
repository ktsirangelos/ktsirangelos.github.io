{
  "name": "k",
  "version": "1.0.0",
  "description": "personal website",
  "scripts": {
    "build": "npx parcel build --no-source-maps index.html",
    "remove": "cd ../ && rm -f *.js *.css *.html *.png *.ttf && rm -rf favicon/",
    "copy": "cp -r dist/* ../",
    "build:full": "npm run build && npm run remove && npm run copy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "konst",
  "license": "ISC",
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.9.3",
    "@parcel/transformer-webmanifest": "^2.9.3",
    "parcel": "^2.9.3",
    "sass": "^1.62.1"
  }
}
